name: Python Linting
on: [pull_request, push]

jobs:
  ruff:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v2
    - name: Install Ruff
      run: pip install ruff
    - name: Run Ruff
      run: ruff check .

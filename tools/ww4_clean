#!/bin/bash
#      +--------------------------------------------------------+
#      | WAVEWATCH IV, open source, code management by NOAA/NWS |
#      +--------------------------------------------------------+
#
# @file ww4_clean
# @brief Shell wrapper for the WAVEWATCH IV (WW4) clean tool.
# @details This script identifies the repository root and executes the
#          underlying Python clean tool.
#
# Usage
# -----
# Run the clean tool from any directory within the clone:
#    ww4_clean
#
# @copyright © 2026 National Weather Service, National Oceanic and Atmospheric
#               Administration. WAVEWATCH IV (TM) and WW4 (TM) are trademarks
#               of the National Weather Service.
# @author Aldgisl, Hendrik Tolman (Initial, 2026-03-27)

# Identify the repository root (parent of the directory containing this script)
TOOLS_DIR=$(cd "$(dirname "$0")" && pwd)
ACTIVE_CLONE=$(cd "$TOOLS_DIR/.." && pwd)

CLEAN_TOOL="$ACTIVE_CLONE/tools/ww4_clean.py"

if [ ! -f "$CLEAN_TOOL" ]; then
    echo "Error: Clean tool not found at $CLEAN_TOOL"
    exit 1
fi

python3 "$CLEAN_TOOL" "$@"

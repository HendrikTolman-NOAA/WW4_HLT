# @file ww4_compile_config.yaml
# @brief YAML template for WAVEWATCH IV (WW4) compilation configuration.
# @copyright © 2026 National Weather Service, National Oceanic and Atmospheric Administration, U.S. Federal Government.
#
# NWS often uses Generative AI (GenAI) for code development and refactoring. Whenever GenAI is used, NWS requires a full human review of code before it is added to its repositories
# @details This file provides a template for configuring the compiler and
#          compilation options for WAVEWATCH IV.
#
#          To use this file, copy it to the root directory of the repository
#          or the directory from which the executable is run.
#
#          Note: All YAML files in the root directory of the clone are
#          automatically ignored by Git. This allows for local testing without
#          adding customized input files to the repository.

compiler:
  # Name of the C++ compiler (e.g., g++, clang++, icpx, cl, CC)
  name: "g++"

  # Compilation options/flags
  options: "-O3 -Wall -Wextra -std=c++20"

  # Enable strict compiler warnings and treat as errors (true/false)
  strict_warnings: false

  # Enable address and undefined behavior sanitizers (true/false)
  use_sanitizers: false
